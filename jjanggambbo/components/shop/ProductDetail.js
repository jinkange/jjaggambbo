import React from 'react';
import { View, Text, Button } from 'react-native';

const ProductDetail = ({ navigation }) => {
  return (
    <View>
      <Text>ProductDetail Content</Text>
    </View>
  );
}

export default ProductDetail;